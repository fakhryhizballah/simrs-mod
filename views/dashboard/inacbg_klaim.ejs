<div class="antialiased bg-gray-50 text-gray-800 p-6">
    <div class="max-w-7xl mx-auto">
        
        <div class="mb-6 bg-white p-4 rounded-lg shadow-sm border border-gray-200">
            <h2 class="text-xl font-bold mb-4 text-gray-700">Data Registrasi Pasien</h2>
            <form class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <div>
                    <label class="block text-xs font-semibold uppercase text-gray-500 mb-1">Keyword (No. RM)</label>
                    <input type="text" id="keyword" value="397693" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm p-2 border">
                </div>
                <div>
                    <label class="block text-xs font-semibold uppercase text-gray-500 mb-1">Tanggal Awal</label>
                    <input type="date" id="tanggalawal" value="2024-02-01" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm p-2 border">
                </div>
                <div>
                    <label class="block text-xs font-semibold uppercase text-gray-500 mb-1">Tanggal Akhir</label>
                    <input type="date" id="tanggalakhir" value="2024-02-01" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm p-2 border">
                </div>
                <div>
                    <label class="block text-xs font-semibold uppercase text-gray-500 mb-1">Status Rawat</label>
                    <select id="statusRanap" class="w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm p-2 border">
                        <option value="Ranap">Ranap</option>
                        <option value="Ralan">Ralan</option>
                        <option value="semua">Semua</option>
                    </select>
                </div>

                <div class="mt-4 mb-6">
                    <button type="button" id="cari" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                        Cari
                    </button>
                </div>
            </form>
        </div>
        <div class="m-4 flex justify-between items-center text-sm text-gray-500" id="paginationtop">
           
        </div>
        <div class="overflow-x-auto bg-white shadow-md rounded-lg border border-gray-200">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-100">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">No. Rawat</th>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">Pasien</th>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">Tgl / Jam</th>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">Poliklinik / Dokter</th>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">Penjamin</th>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">Diagnosa / Prosedur</th>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">Status</th>
                        <th class="px-4 py-3 text-left text-xs font-bold text-gray-600 uppercase">Aksi</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200" id="dataklaim">
                </tbody>
            </table>
        </div>
        
        <div class="mt-4 flex justify-between items-center text-sm text-gray-500" id="pagination">
        </div>
    </div>
</div>